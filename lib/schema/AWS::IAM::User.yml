---
type: map
mapping:
  Type:
    type: str
    pattern: /AWS::IAM::User/
  Properties:
    type: map
    mapping:
      Groups:
        type:   seq
        required: no
        sequence:
          - type:   any
      ManagedPolicyArns:
        type:   seq
        required: no
        sequence:
          - type:   any
      Policies:
        type:   seq
        required: no
        sequence:
          - type:   any
      =:
        type: any
  =:
    type: any
